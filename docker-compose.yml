services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - uploads:/app/uploads
    environment:
      - CAUSAL_EFFECT_API_TOKEN=
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  uploads:
